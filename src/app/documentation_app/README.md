## Run Locally

**Prerequisites:** Node.js 24.x


1. Install dependencies:
   `npm install`
2. Configure environment variables as needed using [.env.example](.env.example)
3. Run the app:
   `npm run dev`
